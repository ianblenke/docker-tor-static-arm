# docker-tor-static-arm

To build a tor static binary, do:

    docker-compose up

When this finishes, check the `outputs/` directory, you will now see a `tor` executable that is statically compiled.

    ls -la outputs/tor

